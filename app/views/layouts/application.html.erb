<!DOCTYPE html>
<html>
  <head>
    <title>Technicaltest</title>
    <%= csrf_meta_tags %>
    <link href="/assets/application.css" media="all" rel="stylesheet" data-turbolinks-track="true" />
    <script src="/assets/application.js" data-turbolinks-track => true ></script>
  </head>

  <body>
    <p class="navbar-text pull-right">
    <% if user_signed_in? %>
        Logged in as <strong><%= current_user.email %></strong>.
        <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %>
    <% else %>

        <%= link_to "Login", new_user_session_path, :class => 'navbar-link'  %>
    <% end %>
    </p>
    <ul class="nav">
        <li class="active"><a href="/api_keys">APIKeys</a></li>
        <li class="active"><a href="/api_keys/0/form_duplicate">Duplicates</a></li>
    </ul>
      <% if notice %>
          <p class="alert alert-success"><%= notice %></p>
      <% end %>
      <% if alert %>
          <p class="alert alert-danger"><%= alert %></p>
      <% end %>

    <%= yield %>
  </body>
</html>
